#include "main.h"
/**
 * _islower - Return 1 if c is lowcase else return 0
 */

int _islower(int c)
{
	int lower;
	int counter;

	lower = 0;
	counter = 'a';
	while (counter <= 'z')
	{
		if (c == 'c')
			return (1);
		else
			return (lower);
		counter++;
	}	
}

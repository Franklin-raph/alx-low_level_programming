#include "main.h"

/**
 * mul - Returns the multiplication of two numbers
 * @a: the digit to be multiplied
 * @b: the digit to be multiplied
 *
 * Return: The multiplication of the two numbers
 */

int mul(int a, int b)
{
	return (a * b);
}
